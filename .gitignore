bakaWFS
bakaWFS.exe
certificate.crt
private.key

# 运行时配置（首次启动自动生成，含 secret / 账号密码，不入库）
config.yaml
users.yaml

# 运行时产物
.uploads/
.thumbcache/

# 文件存储目录（含软链到实际数据）
files/

# 本地部署前端（vendor 本地化版，含不可分发的字体子集），仅本机使用
rpihtml/
